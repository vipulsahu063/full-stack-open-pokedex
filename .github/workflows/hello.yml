name: Hello World!

on:
  push: 
    branches:
      - main

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: |
          echo "Hello World!"
    
      - name: Now it is
        run: |
          ls
    
      - name: Directory content
        run: |
          date
